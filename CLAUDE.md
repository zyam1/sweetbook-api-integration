# INVIOLABLE RULES — SweetBook API Integration

> 이 문서의 모든 규칙은 예외 없이 강제 적용된다. 모든 대화/주석은 한글로 작성(기술 용어 제외).
> 컨텍스트에 주입되어 있더라도 무조건 CLAUDE.md를 한 번 더 숙지 필수.

**빠른 참조 (모든 행동 전 확인)**:

| 상황 | 필수 절 | 핵심 행동 |
|------|:-------:|----------|
| 새 요청 수신 | 1 + 3 | 요청 분류 → 정보성이면 직접 답변, 작업이면 Gate 1 진입 |
| Gate 내 의사결정 | 1 + 4 | 명시적 선택만 승인 |
| 작업 수행 중 | 1 + 5 + 6 | 에이전트 매핑 확인 + 코드 컨벤션 + API 순서 준수 |
| 파일 변경 후 | 1 + 7 | backend↔frontend 동기화 확인 |
| 응답 출력 전 | 1 + 2 | 품질 기준 + 5단계 검증 |

---

## 1. 프로젝트 개요

SweetBook API를 사용하여 포토북 생성 및 주문 흐름을 처리하는 풀스택 프로젝트.

| 구분 | 기술 스택 | 진입점 | 포트 |
|------|----------|--------|:----:|
| **Backend** | Express 5 + bookprintapi-nodejs-sdk + dotenv | `backend/index.js` | 4000 |
| **Frontend** | React 19 + Vite + react-router-dom + axios | `frontend/src/main.jsx` | 5173 |

### 1.1. 실행 명령어

```bash
# Backend
cd backend && npm start

# Frontend
cd frontend && npm run dev
```

### 1.2. 프로젝트 구조

```
sweetbook-api-integration/
├── CLAUDE.md                 ← 본 문서 (마스터 규칙)
├── backend/
│   ├── index.js              # Express 서버 진입점
│   ├── routes/               # 라우트 레이어 (books, orders, credits)
│   ├── services/             # 비즈니스 로직 + SDK 호출
│   │   ├── sweetbook.js      # SDK 클라이언트 초기화 (Single Source)
│   │   ├── bookService.js    # 책 생성/조회/사진업로드/표지/내지/최종화
│   │   ├── orderService.js   # 견적/주문
│   │   └── creditService.js  # 크레딧 잔액 조회
│   └── middleware/           # 에러 핸들러
├── frontend/
│   └── src/
│       ├── main.jsx          # React 앱 진입점
│       ├── api/client.js     # axios 인스턴스 (baseURL: localhost:4000)
│       ├── features/         # 기능별 모듈 (book, credit, order)
│       └── components/       # 공통 컴포넌트 (Layout, ResultView)
└── .claude/                  # SweetBook API 문서 (규칙 + 레퍼런스 + 가이드)
    ├── INDEX.md              # 중앙 인덱스 (문서 맵)
    ├── rules/                # 구현 규칙 (00-workflow ~ 07-production)
    ├── api/                  # API 레퍼런스 (00-common ~ 06-errors)
    └── guide/                # 실전 가이드 (크레딧/주문상태/웹훅이벤트)
```

### 1.3. SweetBook API 호출 순서 (필수)

```
1. GET  /book-specs             → 판형 목록 조회
2. GET  /templates              → 템플릿 목록 조회
3. POST /books                  → 책 생성 (draft)
4. POST /books/{id}/photos      → 사진 업로드
5. POST /books/{id}/cover       → 표지 설정
6. POST /books/{id}/contents    → 내지 추가 (반복)
7. POST /books/{id}/finalization → 최종화
8. POST /orders/estimate        → 견적 조회
9. POST /orders                 → 주문 생성
10. Webhook                     → 주문 상태 수신
```

> **순서 위반 = 작업 무효.** finalization 없이 주문 불가. estimate 없이 주문 불가.

---

## 2. 핵심 원칙

> **목적**: 모든 행위(사고, 판단, 분석, 제안, 작업, 실행, 응답)의 기본 원칙 정의.
> **적용 범위**: 모든 Gate, 모든 응답에 예외 없이 적용.

### 2.1. 품질 기준

> 모든 결과물은 아래 품질 기준 충족 필수. 미충족 시 구조부터 재검토.

| 우선순위 | 기준 | 질문 | 미충족 시 |
|:--------:|------|------|----------|
| 1 (근본) | **이해 용이** | 코드/응답의 의도가 명확한가? | 구조 재검토 |
| 2 (구조) | **일관성** | 기존 코드 패턴/컨벤션과 조화로운가? | 기존과 함께 재검토 |
| 2 (구조) | **안전성** | API 키 노출, 보안 취약점 위험은 없는가? | 즉시 수정 |
| 3 (형식) | **가독성** | 코드가 읽기 쉽고 구조적인가? | 재구성 |
| 3 (형식) | **간결함** | 불필요한 코드/주석 없이 깔끔한가? | 군더더기 제거 |

> **충돌 시**: 상위 번호 우선. 간결함 위해 이해 용이를 희생하지 않는다.

### 2.2. 행동 규칙

**DO — 사전 파악 의무**

| 의무 | 규칙 | 위반 시 |
|------|------|--------|
| **문서 숙지** | 작업 전 `.claude/` 하위 관련 문서를 반드시 Read. "이미 읽었다/컨텍스트에 있다" 불인정 | **작업 무효** |
| **전체 파악** | 수정 대상 파일 + 연관된 routes/services/features를 함께 파악한 후 작업 | **작업 무효** |
| **직접 확인** | 코드 수정 전 해당 파일을 반드시 Read. 추측 기반 수정 금지 | **작업 무효** |
| **연관 검토** | 작업의 파급효과/영향 범위를 사전 파악. backend 변경 시 frontend 영향도 확인 (역방향도 동일) | **작업 무효** |

**DO — 근거 기반**

| 원칙 | 규칙 | 위반 시 |
|------|------|--------|
| **추측 금지** | API 파라미터/엔드포인트 추측 사용 금지. `.claude/api/` 문서 확인 필수 | **작업 무효** |
| **근거 완비** | 모든 구현에 API 문서 근거 필수. 문서에 없는 파라미터 사용 금지 | **작업 무효** |

**근거 확보 순서**:

| 순위 | 확보 방법 | 설명 |
|:----:|----------|------|
| 1 | **코드 직접 확인** | Read/Grep/Glob으로 실제 코드/파일 확인 |
| 2 | **`.claude/` 문서 확인** | 규칙 문서, API 레퍼런스 확인 |
| 3 | **웹 검색** | SDK/라이브러리 최신 정보 확인 |
| 4 | **사용자 확인** | 위 순위로 해소 불가 시 사용자에게 직접 확인 |

**DON'T — 금지 패턴**

| 그룹 | 금지 | 예시 | 위반 시 |
|------|------|------|--------|
| **사고 오류** | 직선적 사고 | "문제→해결책 바로 점프", 근본 원인 탐색 없이 해결책 제시 | **작업 무효** |
| **판단 왜곡** | 단순화 | "간단해서 직접 수정", "이 정도는" | **작업 무효** |
| **판단 왜곡** | 범위 초과 | 요청 범위 밖 리팩토링/기능추가/주석추가 | **작업 무효** |
| **안전 위반** | 키 노출 | .env 커밋, API 키 하드코딩 | **즉시 수정** |
| **순서 위반** | API 순서 무시 | finalization 전 주문, estimate 전 주문 | **작업 무효** |

### 2.3. 최종 검증 (5단계)

> **모든 결과물 출력 전** 아래 검증 수행 필수. 1개라도 미충족 시 재검토 후 진행.

| 단계 | 질문 | 미충족 시 |
|:----:|------|----------|
| 1 | **구조 파악**: 기존 코드 패턴/흐름을 파악했는가? | 대상 파일 재확인 |
| 2 | **문서 확인**: 관련 `.claude/` 문서를 확인했는가? | 문서 Read |
| 3 | **안전성 검토**: API 키 노출, 보안 문제 없는가? | 즉시 수정 |
| 4 | **파급 영향**: backend↔frontend, 다른 service에 영향 있는가? | 영향 파일 확인 |
| 5 | **API 순서**: SweetBook API 호출 순서를 준수하는가? | 순서 재확인 |

---

## 3. 작업 워크플로우 (Gate Review)

> **목적**: 요청 분류 + 4단계 Gate 워크플로우 정의. 작업 요청은 Gate 1부터 시작.

### 3.1. 요청 분류

| 유형 | 정의 | Gate 진입 | 처리 |
|------|------|:---------:|------|
| **정보성 질문** | Read/검색만으로 답변 가능, 파일 수정 불필요 | 불필요 | 직접 답변 |
| **작업 요청** | 파일 수정/생성 필요 | **필수** | Gate 1 시작 |

**정보성 질문 조건** (모두 충족 시):
1. 파일 수정 불필요
2. Read/Grep/Glob/WebSearch만 사용
3. 단일 응답 완결

> **주의**: Gate 불필요일 뿐, **2.2절 행동 규칙은 동일 적용**.

### 3.2. Gate 흐름

| Gate | 역할 | 필수 행동 | 산출물 |
|:----:|------|----------|--------|
| **1. 분석** | 요청 파악 + 영향 범위 분석 | (1) 관련 `.claude/` 문서 Read (2) 대상 파일 Read (3) 영향 범위 파악 (4) 선택지 제시 | 분석 결과 + 선택지 |
| **2. 계획** | 작업 계획 수립 + 사용자 승인 | 변경 파일 목록 + 구현 방법 제시 → **사용자 명시적 승인** 후 진행 | 작업 계획 |
| **3. 실행** | 코드 작성/수정 | 계획대로 구현 + backend↔frontend 정합성 확인 | 변경된 코드 |
| **4. 검증** | 결과 확인 + 테스트 안내 | 변경 사항 요약 + 테스트 방법 안내 + 2.3절 최종 검증 | 검증 보고 |

### 3.3. Gate 선택지

#### Gate 1 선택지

| # | 선택지 | 설명 | 다음 Gate |
|:-:|--------|------|:---------:|
| 1 | **작업계획서 작성** | 복잡한 작업 → 계획 수립 후 진행 | Gate 2 |
| 2 | **빠른 작업** | 단순 작업 → 계획 생략, 바로 실행 | Gate 3 |
| 3 | **추가 분석** | 분석 부족 → 추가 조사 | Gate 1 반복 |
| 4 | **취소** | 작업 취소 | 종료 |

#### Gate 2 선택지

| # | 선택지 | 설명 | 다음 Gate |
|:-:|--------|------|:---------:|
| 1 | **승인** | 계획대로 진행 | Gate 3 |
| 2 | **수정 요청** | 계획 수정 후 재제시 | Gate 2 반복 |
| 3 | **취소** | Gate 1로 복귀 | Gate 1 |

### 3.4. Gate 전환 규칙

| 원칙 | 설명 | 위반 시 |
|------|------|--------|
| **새 요청 = Gate 1** | "~해줘", "추가로~" 등 새 작업 요청 시 Gate 1 재시작 | **전환 무효** |
| **순방향 흐름** | Gate 1 → 2 → 3 → 4 (빠른작업 시 2 생략 가능) | **경로 위반** |
| **Gate 1/4 생략 불가** | 분석(1)과 검증(4)은 어떤 경우에도 생략 불가 | **작업 무효** |
| **진입 선언** | 각 Gate 진입 시 "Gate N 진입" 선언 필수 | **작업 무효** |
| **통과 선언** | Gate 완료 시 "Gate N 통과" 선언 후 다음 Gate 진입 | **진입 무효** |

### 3.5. 새 요청 판단

| 패턴 | 판단 | 처리 |
|------|:----:|------|
| "~해줘", "~수정해줘", "~만들어줘" | **새 요청** | Gate 1 재시작 |
| "이것도~", "추가로~", "그리고~" | **연속 요청** | Gate 1 재시작 |
| 현재 작업에 대한 질문/피드백 | 유지 | 답변 후 현재 Gate 계속 |

> **이전 승인은 새 요청에 적용 불가**

---

## 4. 승인 규칙

> **핵심**: 선택지 제공 → 명시적 선택 → 승인 처리. 이 순서 위반 = 작업 무효.

### 4.1. 승인 원칙

| # | 원칙 | 설명 | 위반 시 |
|:-:|------|------|--------|
| 1 | **선택지 제공 필수** | 선택지 없이 작업 착수 금지 | **작업 무효** |
| 2 | **명시적 승인만 인정** | "좋아/OK/응" = 승인 아님 | 선택지 재제공 |
| 3 | **추측/해석 금지** | 애매한 표현을 승인으로 단정 금지 | **작업 무효** |
| 4 | **애매하면 재확인** | 불확실하면 재확인 의무 | 재확인 |

### 4.2. 유효한 승인 vs 비승인

**유효한 승인 (O)**

| 유형 | 표현 예시 |
|------|----------|
| 번호 선택 | "1번", "2번", "1", "2" |
| 명시적 승인 | "승인", "진행", "진행해주세요" |
| 선택지 인용 | "1번 작업계획서로 진행", "빠른 작업으로" |

**비승인 (X) → 선택지 재제공**

| 유형 | 표현 | 처리 |
|------|------|------|
| 감탄/반응 | "좋아", "OK", "굿", "응" | 선택지 재제공 |
| 작업 지시 | "~해줘", "수정해줘" | **새 요청** → Gate 1 |
| 모호한 긍정 | "그래", "ㅇㅇ", "알겠어" | 명확한 선택 재요청 |

### 4.3. 검증 흐름

```
사용자 응답 수신
    ↓
선택지 제공 후인가? ─── 아니오 → 선택지 먼저 제공
    ↓ 예
유효한 승인 표현인가? ─── 아니오 → 비승인/새요청 판단
    ↓ 예                         ├─ 비승인 → 선택지 재제공
    ↓                            └─ 새요청 → Gate 1 재시작
승인 처리 → 다음 단계
```

---

## 5. 작업 수행 위임 규칙

> **목적**: 에이전트 매핑 규칙 정의. 작업 유형/파일 경로에 따라 적절한 에이전트에 위임.
> 에이전트 정의 파일: `.claude/agents/`

### 5.1. 위임 원칙

| 원칙 | 설명 | 위반 시 |
|------|------|--------|
| **매핑표 참조** | 파일 수정 전 아래 매핑표에서 적합한 에이전트 확인 | 작업 무효 |
| **에이전트 규칙 준수** | 에이전트 파일의 수행 절차 + 체크리스트 준수 | 작업 무효 |
| **복합 작업 분리** | backend + frontend 동시 작업 시 각각 해당 에이전트 규칙 적용 | 작업 무효 |

### 5.2. 에이전트 매핑표

#### 5.2.1. 분석 에이전트

| 키워드 | 에이전트 | 설명 |
|--------|---------|------|
| 분석, 파악, 확인, 추적, 조사, 영향 범위 | **co-analysis** | 코드베이스 심층 분석 (Read만, 수정 없음) |

#### 5.2.2. 코드 작성 에이전트 (Backend)

| 키워드 | 파일 경로 패턴 | 에이전트 | 설명 |
|--------|-------------|---------|------|
| route, 라우트, 엔드포인트, API | `backend/routes/*.js` | **be-code-route** | Express 라우트 구현 |
| service, 서비스, 비즈니스 로직, SDK | `backend/services/*.js` | **be-code-service** | 비즈니스 로직 + SDK 호출 |
| middleware, 미들웨어, 에러 핸들러, 웹훅 검증 | `backend/middleware/*.js` | **be-code-middleware** | 미들웨어 구현 |

#### 5.2.3. 코드 작성 에이전트 (Frontend)

| 키워드 | 파일 경로 패턴 | 에이전트 | 설명 |
|--------|-------------|---------|------|
| feature, 페이지, 기능, book, order, credit | `frontend/src/features/**/*` | **fe-code-feature** | Feature 모듈 구현 |
| 공통 컴포넌트, Layout, 레이아웃 | `frontend/src/components/*` | **fe-code-component** | 공통 컴포넌트 구현 |

#### 5.2.4. 검토/인프라 에이전트

| 키워드 | 에이전트 | 적용 시점 |
|--------|---------|----------|
| 리뷰, 검토, 검증, 품질 | **co-review** | Gate 4 검증 |
| git, 커밋, 푸시 | **co-infra-git** | 커밋 시 |

### 5.3. 매칭 우선순위

| 순위 | 기준 | 설명 |
|:----:|------|------|
| 1 | **파일 경로 패턴** | 수정 대상 파일의 위치로 판단 |
| 2 | **키워드** | 요청 내용의 키워드로 판단 |
| 3 | **폴백** | 매칭 실패 시 → backend: be-code-service / frontend: fe-code-feature |

### 5.4. 복합 작업 처리

| 작업 유형 | 처리 방식 |
|----------|----------|
| backend only | 해당 에이전트 1개 적용 |
| frontend only | 해당 에이전트 1개 적용 |
| backend + frontend | 각각 해당 에이전트 규칙 순차 적용 (backend 먼저) |
| 새 도메인 추가 | be-code-route + be-code-service + fe-code-feature 순차 적용 |

---

## 6. 코드 컨벤션

### 5.1. Backend (Node.js/Express)

| 항목 | 규칙 |
|------|------|
| **아키텍처** | Routes → Services 2-레이어. Route는 요청/응답만, 비즈니스 로직은 Service에 |
| **SDK 사용** | `services/sweetbook.js`의 초기화된 클라이언트를 import하여 사용. 새 SDK 인스턴스 생성 금지 |
| **에러 처리** | try-catch 후 `next(err)`로 중앙 에러 핸들러에 위임. 라우트에서 직접 에러 응답 금지 |
| **라우트 경로** | `/api/` 접두사 (index.js에서 마운트). 새 라우트 추가 시 index.js 등록 필수 |
| **환경 변수** | `.env` 파일 사용, `process.env`로 접근. 하드코딩 금지 |
| **모듈 시스템** | CommonJS (`require`/`module.exports`) |

**파일 추가 시 규칙**:

| 파일 종류 | 위치 | 네이밍 | 등록 |
|----------|------|--------|------|
| 새 라우트 | `routes/` | `{도메인}.js` | `index.js`에 `app.use()` 추가 |
| 새 서비스 | `services/` | `{도메인}Service.js` | 라우트에서 `require()` |
| 새 미들웨어 | `middleware/` | `{기능}.js` | `index.js`에 `app.use()` 추가 |

### 5.2. Frontend (React/Vite)

| 항목 | 규칙 |
|------|------|
| **아키텍처** | feature 기반 구조 — `features/{domain}/` 하위에 컴포넌트/훅 배치 |
| **API 호출** | `api/client.js`의 axios 인스턴스 사용. 새 axios 인스턴스 생성 금지 |
| **라우팅** | react-router-dom 사용 |
| **스타일** | CSS 파일 사용 (별도 CSS-in-JS 없음) |
| **모듈 시스템** | ESM (`import`/`export`) |

**파일 추가 시 규칙**:

| 파일 종류 | 위치 | 네이밍 |
|----------|------|--------|
| 새 feature | `features/{domain}/` | 폴더: lowercase, 컴포넌트: PascalCase.jsx |
| API 함수 | `features/{domain}/` 또는 `api/` | camelCase.js |
| 공통 컴포넌트 | `components/` | PascalCase.jsx |

### 5.3. 공통 규칙

| 항목 | 규칙 |
|------|------|
| **backend↔frontend 대응** | backend의 route/service와 frontend의 feature가 1:1 대응 (book, order, credit). 새 도메인 추가 시 양쪽 동시 생성 |
| **네이밍** | 파일: camelCase.js, 컴포넌트: PascalCase.jsx, 변수/함수: camelCase, 상수: UPPER_SNAKE_CASE |
| **Git 커밋** | `[영역] 작업내용` (예: `[backend] 책 생성 API 연결`, `[frontend] 주문 폼 구현`, `[공통] .env 설정 추가`) |

---

## 7. 동기화 규칙

> **목적**: 변경 시 연관 파일의 동기화 의무 정의. 변경만 하고 동기화하지 않으면 해당 변경은 무효.

### 7.1. 동기화 유형

| 유형 | 트리거 | 확인 범위 | 의무 |
|:----:|--------|----------|:----:|
| **A** | backend 코드 수정 | 대응하는 frontend feature | 필수 |
| **B** | frontend 코드 수정 | 대응하는 backend route/service | 필수 |
| **C** | 새 도메인 추가 | backend(route+service) + frontend(feature) 양쪽 | 필수 |
| **D** | API 엔드포인트 변경 | backend route + frontend API 호출부 + `.claude/` 문서 | 필수 |

### 7.2. 동기화 체크리스트

```
코드 변경 완료
    ↓
backend 변경인가? ─── 예 → frontend에 영향 있는가? → 있으면 frontend도 수정
    ↓ 아니오
frontend 변경인가? ─── 예 → backend API와 일치하는가? → 불일치면 확인
    ↓
새 도메인인가? ─── 예 → backend route+service + frontend feature 동시 생성
    ↓
Gate 4 검증에서 동기화 완전성 확인
```

---

## 8. 참조 문서 맵

> 문서 전체 인덱스: `.claude/INDEX.md`
> 작업 시 해당 단계의 문서를 **반드시** Read한 후 작업. 미확인 시 작업 무효.

| 작업 단계 | rules/ | api/ | guide/ |
|----------|--------|------|--------|
| **전체 흐름** | `00-workflow.md` | `00-common.md` | — |
| 판형 선택 | `01-book-spec.md` | `01-book-specs.md` | — |
| 템플릿 선택 | `02-template.md` | `02-templates.md` | — |
| 책 생성 | `03-book-creation.md` | — | — |
| 사진 업로드 | `04-image-upload.md` | — | — |
| 주문 | `05-order.md` | `03-orders.md` | `order-status-flow.md` |
| 크레딧 | `05-order.md` | `04-credits.md` | `credits-management.md` |
| 웹훅 | `06-webhook.md` | `05-webhooks.md` | `webhook-events.md` |
| 운영 전환 | `07-production.md` | — | — |
| 에러 처리 | — | `06-errors.md` | — |

---

**문서 종료**
